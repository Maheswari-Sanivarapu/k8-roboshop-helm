apiVersion: v1 
kind: Service
metadata:
  name: mongodb
  namespace: roboshop
  labels:
    project: roboshop
    component: mongodb
    tier: DB
spec:
  selector:
    project: roboshop
    component: mongodb
    tier: DB
  ports:
  - protocol: TCP
    port: 27017
    targetPort: 27017